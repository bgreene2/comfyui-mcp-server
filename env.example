# ComfyUI MCP Server Environment Variables

# Hostname or IP address of ComfyUI API
COMFYUI_HOST=127.0.0.1

# Port number of ComfyUI API
COMFYUI_PORT=8188

# Directory where generated images will be saved
OUTPUT_DIR=./output

# URL where generated images will be served from
IMAGE_APP_BASE_URL=http://127.0.0.1:8081/view

# Name of the ComfyUI workflow (without extension)
COMFYUI_WORKFLOW_NAME=flux-krea

# (Optional) Directory where the server resides (parent directory of 'server.py' and 'workflows/')
# WORKING_DIR=/path/to/directory/containing/server
